hbVersion: '1.0'
type: metric
name: quantity per order
config:
  metricType: timeseries
  dataModel: m::sales
  measureId: quantity_per_order
  dimensionId: timestamp
  granularity: month
  displayOptions:
    sparkline: true
    numPeriodsToDisplay: 12
    displayLastPartialPeriod: false
